import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("NutriTrainer")
                .font(.largeTitle.bold())
            Text("iPhone 17 Pro Max + Apple Watch")
            Text("Control de comidas y macros")
                .padding()
        }
    }
}
